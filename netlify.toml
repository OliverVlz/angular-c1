
[build]
  base = "project"
  command = "npm ci && npm run build -- --configuration=production"
  publish = "dist/project/browser"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200